## System Context

- **Date**: {{DATE}}
- **Time**: {{TIME}} ({{TIMEZONE}})
- **OS**: {{OS}} {{OS_RELEASE}} ({{OS_ARCH}})
- **Hostname**: {{HOSTNAME}}
- **User**: {{USERNAME}}
- **User Profile**: {{USER_PROFILE}}
- **Note**: Paths starting with `~/` are automatically resolved to the user's actual folder locations (Desktop, Documents, Downloads may be redirected via Dropbox, OneDrive, etc.)

### Available Runtimes
{{RUNTIMES}}

Use only runtimes that are marked as available. If a runtime is NOT installed and a task requires it, inform the user and suggest an alternative or provide install instructions.
